const pdflib = window.pdfjsLib;

const AnnotationLayer = pdflib.AnnotationLayer;
const RenderingCancelledException = pdflib.RenderingCancelledException;
const PasswordResponses = pdflib.PasswordResponses;
const removeNullCharacters = pdflib.removeNullCharacters;
const getFilenameFromUrl = pdflib.getFilenameFromUrl;
const createPromiseCapability = pdflib.createPromiseCapability;
const PDFDateString = pdflib.PDFDateString;
const LinkTarget = pdflib.LinkTarget;
const CMapCompressionType = pdflib.CMapCompressionType;
const GlobalWorkerOptions = pdflib.GlobalWorkerOptions;
const InvalidPDFException = pdflib.InvalidPDFException;
const LoopbackPort = pdflib.LoopbackPort;
const MissingPDFException = pdflib.MissingPDFException;
const OPS = pdflib.OPS;
const PDFDataRangeTransport = pdflib.PDFDataRangeTransport;
const PDFWorker = pdflib.PDFWorker;
const PermissionFlag = pdflib.PermissionFlag;
const SVGGraphics = pdflib.SVGGraphics;
const UNSUPPORTED_FEATURES = pdflib.UNSUPPORTED_FEATURES;
const UnexpectedResponseException = pdflib.UnexpectedResponseException;
const Util = pdflib.Util;
const VerbosityLevel = pdflib.VerbosityLevel;
const addLinkAttributes = pdflib.addLinkAttributes;
const apiCompatibilityParams = pdflib.apiCompatibilityParams;
const build = pdflib.build;
const createObjectURL = pdflib.createObjectURL;
const createValidAbsoluteUrl = pdflib.createValidAbsoluteUrl;
const getDocument = pdflib.getDocument;
const loadScript = pdflib.loadScript;
const renderTextLayer = pdflib.renderTextLayer;
const shadow = pdflib.shadow;
const version = pdflib.version;

export {
  AnnotationLayer,
  RenderingCancelledException,
  PasswordResponses,
  removeNullCharacters,
  getFilenameFromUrl,
  createPromiseCapability,
  PDFDateString,
  LinkTarget,
  CMapCompressionType,
  GlobalWorkerOptions,
  InvalidPDFException,
  LoopbackPort,
  MissingPDFException,
  OPS,
  PDFDataRangeTransport,
  PDFWorker,
  PermissionFlag,
  SVGGraphics,
  UNSUPPORTED_FEATURES,
  UnexpectedResponseException,
  Util,
  VerbosityLevel,
  addLinkAttributes,
  apiCompatibilityParams,
  build,
  createObjectURL,
  createValidAbsoluteUrl,
  getDocument,
  loadScript,
  renderTextLayer,
  shadow,
  version,
}
